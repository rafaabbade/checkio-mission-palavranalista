def encontra_palavra(lista, palavra):
    # escreva aqui seu código
    return None


print("Example:")
print(encontra_palavra(['eu', 'amo', 'programar'], 'amo'))

# As funções assert são utilizadas para verificar se o resultado da função chamada é o esperado
assert encontra_palavra(['python', 'java', 'ruby', 'c++'], 'python') == True 
assert encontra_palavra(['gato', 'cachorro', 'pássaro', 'leão'], 'elefante') == False 
assert encontra_palavra(['azul', 'vermelho', 'verde', 'amarelo'], 'rosa') == False
    
print("Seu código passou pelos testes, clique em Check para avançar!")
